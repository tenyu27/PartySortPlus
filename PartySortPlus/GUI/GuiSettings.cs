using Dalamud.Bindings.ImGui;

namespace PartySortPlus.GUI
{
    public static class GuiSettings
    {
        public static void Draw() 
        {
            ImGui.TextWrapped("There's no settings here yet, but maybe when the app grows?");
        }
    }
}
